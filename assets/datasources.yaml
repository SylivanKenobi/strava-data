apiVersion: 1
datasources:
  - name: influxdb
    type: influxdb
    isDefault: true
    editable: true
    user: $INFLUXDB_USER
    database: $INFLUXDB_DB
    url: http://$INFLUX_HOST:8086
    secureJsonData:
      password: $INFLUXDB_USER_PASSWORD
